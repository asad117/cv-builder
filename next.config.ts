import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   /* config options here */
// };
const nextConfig = {
  output: 'standalone', // This creates a minimal folder for production
}
export default nextConfig;
